[workspace]
members = [
    "crates/sage",
    "crates/sage-cli",
    "crates/sage-cloudpath",
    "crates/sage-mcp",
]

resolver = "2"

[workspace.package]
version = "0.16.0-beta.1"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
