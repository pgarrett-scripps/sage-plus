[package]
name = "sage-cloudpath"
version.workspace = true
authors = ["Michael Lazear <michaellazear92@gmail.com"]
edition = "2021"
rust-version = "1.88"
description = "A very fast proteomics database search tool"
readme = "README.md"
license = "MIT"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
thiserror = "1.0"
async-compression = { version = "0.3", features = ["tokio", "gzip", "zlib"] }
base64 = "0.13"
bytes = "1.0"
futures = "0.3"
log = "0.4.0"
object_store = { version = "0.14.1", features = ["aws", "gcp", "azure"] }
tokio = { version = "1.0", features = ["fs", "io-util", "rt", "macros"] }
tokio-util = { version = "0.7", features = ["io"] }
quick-xml = { version = "0.31.0", features = ["async-tokio"] }
timsrust = { version = "0.6.4" }
opentfraw = "1.4.0"
rayon = "1.5"
reqwest = { version = "0.13.4", features = ["json", "rustls"], default-features = false }
regex = "1.6"
url = { version = "2", features = ["serde"]}

serde = { version="1.0", features = ["derive"] }
serde_json = "1.0"

sage-core = { path = "../sage" }
parquet = { version = "59.2.0", optional = true, default-features = false, features = ["zstd"] }
